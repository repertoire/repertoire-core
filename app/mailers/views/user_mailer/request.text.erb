Dear <%= @reviewer.full_name %>,

<%= @membership.user.full_name %> (<%= @membership.user.email %>) has requested <%= @membership.role.title %> access to Repertoire.  You are authorized to approve or deny.

<%= ">> #{@membership.user_note}" if @membership.user_note %>

Please log in to review the request as soon as possible:

<%= @link %>